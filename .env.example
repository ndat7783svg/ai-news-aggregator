# Chép file này thành ".env" rồi dán các giá trị thật vào.
# KHÔNG commit file .env (đã có trong .gitignore).

# Anthropic (tóm tắt AI)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Supabase — lấy ở Dashboard > Project Settings > API
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
# service_role key: BÍ MẬT, chỉ dùng cho script ghi dữ liệu (KHÔNG để lộ ra web)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# (Tuỳ chọn) GitHub token — không bắt buộc, chỉ giúp hạn mức API cao hơn.
# GITHUB_TOKEN=ghp_...

# (Tuỳ chọn) Reddit — tạo "app" tại https://www.reddit.com/prefs/apps (loại "web app").
# REDDIT_CLIENT_ID=xxxxxxxxxxxxxx
# REDDIT_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
